include:
  - template: Security/Dependency-Scanning.gitlab-ci.yml

dependency_scanning:
  rules:
    - if: $PIPELINE_TYPE == "scheduled_test"
    - if: $PIPELINE_TYPE != "scheduled_test"
      when: never